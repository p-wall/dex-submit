{
  "name": "dex-submit",
  "version": "0.1.4",
  "description": "Firefox extension that auto-submits Dex login device code flow",
  "private": true,
  "engines": {
    "node": "25.3.0"
  },
  "scripts": {
    "build": "make build",
    "release": "make release"
  },
  "devDependencies": {
    "web-ext": "^8.3.0"
  }
}
